1.Create a for loop that iterates through a list of numbers and prints the square of each number.

2. Write a Python function generate_primes(n) that takes a positive integer `n` as input and returns a list of all prime numbers less than or equal to `n`, using a for loop.